# nullsystem: The Null/Nothing System

The NullSystem implements a null, i.e., no operation, system. This is used primarily as example
code for integrating new systems and testing.

`cmd_repl_test`: This is a test harness for testing multiline CLI input to ensure that skipping comments,
blank lines and white space work.

`reset_test`: Tests the CLI's "RESET" command.